password: xzTXq1rDJQVVAzdv5cHq1TQytTWufAMq

## Решение:
```
cd inhere
ls -a
cat ...Hiding-From-You
```

## Про уровень:
Здесь нужно было перейти в другую директорию, с чем я довольно успешно справилась. А вот с просмотром скрытых файлов возникли небольшие трудности, я сначала написала `-ls`, не сработало потому, что `-` означает опции, т.е что дальше будут идти настройки того, как будет работать команда. А `ls` - это сама команда (содержание текущей директории). В итоге, правильная команда `ls -a` 